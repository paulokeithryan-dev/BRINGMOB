-- =====================================================================
-- ARENA AIR DROP & VOID FALL ENGINE + RAYFIELD (WITH UNLOAD FEATURE)
-- Teleports mobs off arena edges into mid-air so they fall and die
-- =====================================================================

local Services = {
    Players = game:GetService("Players"),
    RunService = game:GetService("RunService"),
    Workspace = game:GetService("Workspace")
}

local LocalPlayer = Services.Players.LocalPlayer

-- Config Matrix
getgenv().BringMobsConfig = getgenv().BringMobsConfig or {
    Enabled = false,                 -- Toggle state
    BringDistance = 300,             -- Radius in studs to search for mobs
    AirDropMode = true,              -- If true, bypasses floor raycast & drops in mid-air
    ForwardOffset = 10,              -- Distance IN FRONT of player (push over edge)
    HeightOffset = -2,               -- Vertical height relative to player (- values = lower)
    SideOffset = 0,                  -- Left (-) or Right (+) offset
    TeleportInterval = 0.25,         -- Pulse speed in seconds
    IgnorePlayers = true,            -- Ignore player characters
    CustomFolderName = "",           -- Target folder in Workspace
    Whitelist = {},                  -- Name whitelist
    Blacklist = {}                   -- Name blacklist
}

local BringLoopConnection = nil
local lastTPTime = 0

-- Multi-Mirror Rayfield Loader
local Rayfield = nil
local rayfieldURLs = {
    "https://raw.githubusercontent.com/shlexware/Rayfield/main/source",
    "https://sirius.menu/rayfield",
    "https://raw.githubusercontent.com/SiriusSoftwareLTD/Rayfield/main/source"
}

for _, url in ipairs(rayfieldURLs) do
    local success, result = pcall(function()
        return loadstring(game:HttpGet(url))()
    end)
    if success and result then
        Rayfield = result
        break
    end
end

if not Rayfield then return end

-- Validate Mob
local function isValidMob(model)
    if not model or not model:IsA("Model") then return false end

    if getgenv().BringMobsConfig.IgnorePlayers and Services.Players:GetPlayerFromCharacter(model) then
        return false
    end

    local humanoid = model:FindFirstChildOfClass("Humanoid")
    if not humanoid or humanoid.Health <= 0 then
        return false
    end

    local rootPart = model.PrimaryPart or model:FindFirstChild("HumanoidRootPart") or model:FindFirstChild("Torso")
    if not rootPart then
        return false
    end

    local modelName = string.lower(model.Name)

    -- Whitelist
    local whitelist = getgenv().BringMobsConfig.Whitelist
    if #whitelist > 0 then
        local matched = false
        for _, name in ipairs(whitelist) do
            if string.find(modelName, string.lower(name)) then
                matched = true
                break
            end
        end
        if not matched then return false end
    end

    -- Blacklist
    local blacklist = getgenv().BringMobsConfig.Blacklist
    if #blacklist > 0 then
        for _, name in ipairs(blacklist) do
            if string.find(modelName, string.lower(name)) then
                return false
            end
        end
    end

    return true, rootPart, humanoid
end

-- Target CFrame calculation
local function getTargetCFrame(playerRoot)
    if getgenv().BringMobsConfig.AirDropMode then
        -- Calculate mid-air position relative to player's facing direction
        -- -ForwardOffset moves the point in front of where your camera/character faces
        return playerRoot.CFrame * CFrame.new(
            getgenv().BringMobsConfig.SideOffset,
            getgenv().BringMobsConfig.HeightOffset,
            -getgenv().BringMobsConfig.ForwardOffset
        )
    else
        -- Ground Raycast Fallback
        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude
        rayParams.FilterDescendantsInstances = {LocalPlayer.Character}

        local rayResult = Services.Workspace:Raycast(playerRoot.Position, Vector3.new(0, -100, 0), rayParams)
        if rayResult then
            return CFrame.new(rayResult.Position + Vector3.new(0, 3, 0))
        else
            return CFrame.new(playerRoot.Position - Vector3.new(0, 5, 0))
        end
    end
end

-- Teleport and drop logic
local function processBringAndDrop()
    if not getgenv().BringMobsConfig.Enabled then return end

    if tick() - lastTPTime < getgenv().BringMobsConfig.TeleportInterval then return end
    lastTPTime = tick()

    local character = LocalPlayer.Character
    local playerRoot = character and character:FindFirstChild("HumanoidRootPart")
    if not playerRoot then return end

    local targetCFrame = getTargetCFrame(playerRoot)

    local searchContainer = Services.Workspace
    if getgenv().BringMobsConfig.CustomFolderName ~= "" then
        local foundFolder = Services.Workspace:FindFirstChild(getgenv().BringMobsConfig.CustomFolderName)
        if foundFolder then searchContainer = foundFolder end
    end

    local function bringMob(mobModel, mobRoot)
        pcall(function()
            local dist = (mobRoot.Position - playerRoot.Position).Magnitude
            if dist <= getgenv().BringMobsConfig.BringDistance then
                
                -- Enable physics collision so gravity acts on them
                mobRoot.CanCollide = true

                -- Apply strong downward velocity so they plummet immediately
                mobRoot.AssemblyLinearVelocity = Vector3.new(0, -60, 0)

                -- Pivot model to target mid-air location
                if mobModel.PrimaryPart then
                    mobModel:PivotTo(targetCFrame)
                else
                    mobRoot.CFrame = targetCFrame
                end
            end
        end)
    end

    for _, obj in ipairs(searchContainer:GetChildren()) do
        local valid, mobRoot = isValidMob(obj)
        if valid then
            bringMob(obj, mobRoot)
        elseif obj:IsA("Folder") then
            for _, nestedObj in ipairs(obj:GetChildren()) do
                local nestedValid, nestedRoot = isValidMob(nestedObj)
                if nestedValid then
                    bringMob(nestedObj, nestedRoot)
                end
            end
        end
    end
end

local function setEngineState(state)
    getgenv().BringMobsConfig.Enabled = state

    if BringLoopConnection then
        BringLoopConnection:Disconnect()
        BringLoopConnection = nil
    end

    if state then
        BringLoopConnection = Services.RunService.Heartbeat:Connect(processBringAndDrop)
    end
end

-- ---------------------------------------------------------------------
-- Rayfield UI Setup
-- ---------------------------------------------------------------------
local Window = Rayfield:CreateWindow({
   Name = "Arena Air Drop Engine",
   LoadingTitle = "Loading Engine...",
   LoadingSubtitle = "by Keith",
   ConfigurationSaving = { Enabled = false },
   Discord = { Enabled = false },
   KeySystem = false
})

local CombatTab = Window:CreateTab("Bring & Drop", 4483362458)

CombatTab:CreateSection("Main Controls")

CombatTab:CreateToggle({
   Name = "Enable Air Drop / Void Fall",
   CurrentValue = false,
   Flag = "AirDropToggle",
   Callback = function(Value)
      setEngineState(Value)
   end,
})

CombatTab:CreateToggle({
   Name = "Air Drop Mode (Bypass Floor Lock)",
   CurrentValue = true,
   Flag = "AirDropModeToggle",
   Callback = function(Value)
      getgenv().BringMobsConfig.AirDropMode = Value
   end,
})

CombatTab:CreateSection("Drop Position Offsets")

CombatTab:CreateSlider({
   Name = "Forward Distance (In Front of You)",
   Range = {0, 50},
   Increment = 1,
   Suffix = "Studs",
   CurrentValue = 10,
   Flag = "ForwardOffsetSlider",
   Callback = function(Value)
      getgenv().BringMobsConfig.ForwardOffset = Value
   end,
})

CombatTab:CreateSlider({
   Name = "Height Offset",
   Range = {-20, 20},
   Increment = 1,
   Suffix = "Studs",
   CurrentValue = -2,
   Flag = "HeightOffsetSlider",
   Callback = function(Value)
      getgenv().BringMobsConfig.HeightOffset = Value
   end,
})

CombatTab:CreateSlider({
   Name = "Scan Radius",
   Range = {50, 1000},
   Increment = 25,
   Suffix = "Studs",
   CurrentValue = 300,
   Flag = "ScanRadiusSlider",
   Callback = function(Value)
      getgenv().BringMobsConfig.BringDistance = Value
   end,
})

CombatTab:CreateSection("Target Filters & Unload")

CombatTab:CreateInput({
   Name = "Custom Workspace Folder",
   PlaceholderText = "e.g., Mobs, Enemies (Leave empty for Workspace)",
   RemoveTextOnFocusLost = false,
   Callback = function(Text)
      getgenv().BringMobsConfig.CustomFolderName = string.gsub(Text, "%s+", "")
   end,
})

CombatTab:CreateButton({
   Name = "Unload / Destroy Script",
   Callback = function()
      setEngineState(false)
      Rayfield:Notify({
         Title = "Script Unloaded",
         Content = "Cleaned up background loops & destroyed UI.",
         Duration = 3,
      })
      task.wait(0.3)
      Rayfield:Destroy()
   end,
})

Rayfield:Notify({
   Title = "Engine Loaded",
   Content = "Air Drop / Arena Edge Engine is active!",
   Duration = 4,
})
